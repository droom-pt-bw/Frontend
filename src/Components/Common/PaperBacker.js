import styled from 'styled-components';
import Paper from '@material-ui/core/Paper';

const PaperBacker = styled(Paper)`
  margin: 0 auto;
  padding: 2rem 3rem;
  max-width: 800px;
`;

export default PaperBacker;